name: skrips
channels:
  - conda-forge
  - defaults
dependencies:
  - xesmf=0.7.0
  - xarray
  - matplotlib
  - cartopy
  - numpy
  - netcdf4
  - cdo
  - python-cdo
  - pip 
  - pip:
      - "f90nml"
      - "docstring_parser"
      - "typer"
      - "pyyaml"
